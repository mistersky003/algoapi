const algosdk = require('algosdk');

function validPhrase (addr) {
    if (algosdk.isValidAddress()) {
        
    }
    /*let pos = getRandomInt(0, keys.length-1);
    return keys[pos];*/
}

module.exports = {getKey}